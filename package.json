{
  "name": "haken-board",
  "version": "0.1.0",
  "private": true,
  "description": "派遣募集ボード（テスト環境用）",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node db.js --seed",
    "reset": "node db.js --reset"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cookie-parser": "^1.4.7",
    "express": "^4.21.2"
  }
}
